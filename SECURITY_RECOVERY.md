# GlobalYouXuan 应急夺回控制权清单

> 本文件只用于安全应急，不参与网站运行，不修改现有业务逻辑。

## 触发条件
发现以下任一情况时立即进入应急模式：
- 管理后台出现非本人操作
- GitHub 出现陌生提交或部署
- Cloudflare DNS / Pages 配置被异常修改
- Supabase 出现陌生管理员、异常数据库修改或密钥泄露
- Telegram 机器人出现非本人控制消息
- 大量异常登录、订单、付款、下载或接口撞击

## 第一阶段：先夺回控制权
1. 锁定主邮箱并更换密码，确认 2FA 仍由本人控制。
2. GitHub：更换密码、撤销陌生会话、撤销未知 Token / GitHub App 权限。
3. Cloudflare：更换密码、撤销陌生会话/API Token，检查 DNS、Pages、WAF 是否被改。
4. Supabase：撤销陌生会话和成员；轮换泄露的 secret/service-role 等真正秘密密钥。
5. Telegram Bot：如怀疑 Token 泄露，立即通过 BotFather 重新生成 Token，旧 Token 作废。

## 第二阶段：阻止继续破坏
- 优先封管理后台和敏感接口，不随意删除生产数据。
- 暂停陌生部署来源。
- 保留日志和异常记录，避免先清日志导致无法追查。
- 不在受污染版本上继续修补。

## 第三阶段：确认干净版本
- 对照 GitHub 最后一个确认正常的 commit。
- 检查该 commit 后的所有提交、文件修改和部署。
- 确认 Supabase schema、Edge Functions、关键配置没有陌生变更。
- 确认 Cloudflare DNS / Pages 项目指向正确仓库和正确分支。

## 第四阶段：恢复
1. 从确认干净的 GitHub commit 重新部署。
2. 使用已经轮换的新密钥重新配置服务端环境变量。
3. 先开放普通前台访问。
4. 再开放登录和会员功能。
5. 最后开放管理后台和高危管理操作。

## 第五阶段：恢复后核验
必须逐项确认：
- 注册 / 登录正常
- 搜索正常
- 免费专区正常
- 下单正常
- 支付提交正常
- 自动核验正常
- paid → delivered → completed 链路正常
- 下载正常
- 客服正常
- 管理后台正常
- 安全监控无持续高风险异常

## 禁止事项
- 不因安全事件临时重写订单、支付、下载状态机。
- 不批量修改现有 RLS / RPC，除非已明确定位为入侵入口并完成影响确认。
- 不使用旧的、已怀疑泄露的 Token / Secret 恢复服务。
- 不覆盖或删除安全日志后再调查。

## 核心原则
**先保住邮箱、GitHub、Cloudflare、Supabase四层控制权，再恢复网站。**

只要根账号、2FA、干净代码版本和密钥轮换权仍在本人手中，就具备夺回网站控制权的基础。