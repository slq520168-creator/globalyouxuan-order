(()=>{
'use strict';
const $=s=>document.querySelector(s),BUCKET='free-zone-media',MAX=52428800;
function esc(s){return String(s??'').replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[m]))}
function toast(text){const d=document.createElement('div');d.className='toast';d.textContent=text;document.body.appendChild(d);setTimeout(()=>d.remove(),2600)}
async function requireAdmin(){const {data}=await window.gyxSupabase.auth.getSession();if(!data?.session){location.replace('admin-login.html');throw new Error('请先登录管理员账号')}return data.session}
function safeName(name){return String(name||'media').replace(/[^a-zA-Z0-9._\-\u4e00-\u9fff]/g,'_').slice(-100)}
function mimeOf(file){const n=String(file.name||'').toLowerCase();if(file.type)return file.type;if(n.endsWith('.mov'))return 'video/quicktime';if(n.endsWith('.mp4'))return 'video/mp4';if(n.endsWith('.m4v'))return 'video/x-m4v';if(n.endsWith('.webm'))return 'video/webm';return 'application/octet-stream'}
function publicUrl(name){return window.gyxSupabase.storage.from(BUCKET).getPublicUrl(name).data.publicUrl}
function mediaHtml(name,meta={}){const url=publicUrl(name),mime=String(meta.mimetype||'');const video=mime.startsWith('video/')||/\.(mov|mp4|m4v|webm)$/i.test(name);const image=mime.startsWith('image/')||/\.(jpg|jpeg|png|webp|gif|heic|heif)$/i.test(name);let preview='';if(video)preview='<video controls playsinline preload="metadata" src="'+esc(url)+'"></video>';else if(image)preview='<img src="'+esc(url)+'" alt="免费专区素材">';return '<article class="freezone-media-item">'+preview+'<div class="freezone-media-meta"><strong>'+esc(name.replace(/^\d+-[a-z0-9]+-/i,''))+'</strong><small>'+esc(mime||'媒体文件')+'</small><a href="'+esc(url)+'" target="_blank" rel="noopener">打开素材</a><button type="button" class="danger-btn" data-freezone-delete="'+esc(name)+'">删除</button></div></article>'}
async function loadLibrary(){const box=$('#freeZoneMediaList');if(!box)return;box.innerHTML='<div class="loading">正在读取已上传素材…</div>';try{await requireAdmin();const {data,error}=await window.gyxSupabase.storage.from(BUCKET).list('',{limit:100,sortBy:{column:'created_at',order:'desc'}});if(error)throw error;const files=(data||[]).filter(x=>x.name&&x.id);box.innerHTML=files.length?'<div class="freezone-media-grid">'+files.map(x=>mediaHtml(x.name,x.metadata||{})).join('')+'</div>':'<div class="empty">暂时还没有上传素材</div>';box.querySelectorAll('[data-freezone-delete]').forEach(b=>b.onclick=()=>removeFile(b.dataset.freezoneDelete))}catch(e){box.innerHTML='<div class="empty">读取失败：'+esc(e.message||e)+'</div>'}}
async function removeFile(name){if(!confirm('确定删除这个免费专区素材？'))return;const {error}=await window.gyxSupabase.storage.from(BUCKET).remove([name]);if(error){toast('删除失败：'+error.message);return}toast('已删除');loadLibrary()}
async function upload(){const input=$('#freeZoneMediaFile'),box=$('#freeZoneUploadStatus'),btn=$('#freeZoneUploadBtn'),file=input?.files?.[0];if(!file){toast('先选择图片或视频');return}const mime=mimeOf(file);if(!/^(image|video)\//.test(mime)){toast('这里只上传图片或视频');return}if(file.size>MAX){toast('单个文件不能超过50MB');return}btn.disabled=true;box.innerHTML='<div class="loading">正在上传到免费专区素材库…</div>';try{await requireAdmin();const name=Date.now()+'-'+crypto.randomUUID().slice(0,8)+'-'+safeName(file.name);const {error}=await window.gyxSupabase.storage.from(BUCKET).upload(name,file,{contentType:mime,upsert:false,cacheControl:'3600'});if(error)throw error;const url=publicUrl(name);box.innerHTML='<div class="notice" style="margin-top:10px">✓ 已上传到免费专区素材库</div><div class="freezone-url">'+esc(url)+'</div>';input.value='';toast('上传成功');await loadLibrary()}catch(e){box.innerHTML='<div class="empty">上传失败：'+esc(e.message||e)+'</div>';toast('上传失败')}finally{btn.disabled=false}}
function init(){const b=$('#freeZoneUploadBtn');if(b)b.addEventListener('click',upload);loadLibrary();document.querySelector('[data-panel="freezone"]')?.addEventListener('click',()=>setTimeout(loadLibrary,0))}
if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',init,{once:true});else init();
})();