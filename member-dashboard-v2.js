(()=>{'use strict';
const db=window.gyxSupabase,I=window.GYXI18N,$=id=>document.getElementById(id),esc=s=>String(s??'').replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot',"'":'&#39;'}[m]));
if(!db)return;
const L={zh:{edit:'修改资料',color:'我的专属颜色',change:'更换头像',total:'共 {{n}} 条',merge:'一键合并累计下单',selectAll:'全选可合并订单',selected:'已选 {{n}} 单 · {{sum}} USDT',merged:'已合并 {{n}} 个订单',included:'包含订单',needTwo:'至少选择 2 个待处理订单',name:'名字',phone:'电话',wechat:'微信',whatsapp:'WhatsApp',tg:'TG',memberNo:'会员编号',account:'账号',joined:'注册时间',lang:'语言',valid:'有效订单',spent:'累计消费'},en:{edit:'Edit profile',color:'My colors',change:'Change photo',total:'{{n}} records',merge:'Merge orders',selectAll:'Select mergeable orders',selected:'{{n}} selected · {{sum}} USDT',merged:'{{n}} orders merged',included:'Orders included',needTwo:'Select at least 2 pending orders',name:'Name',phone:'Phone',wechat:'WeChat',whatsapp:'WhatsApp',tg:'TG',memberNo:'Member ID',account:'Account',joined:'Joined',lang:'Language',valid:'Valid orders',spent:'Total spent'},km:{edit:'កែព័ត៌មាន',color:'ពណ៌ផ្ទាល់ខ្លួន',change:'ប្តូររូប',total:'{{n}} កំណត់ត្រា',merge:'បញ្ចូលការបញ្ជាទិញ',selectAll:'ជ្រើសការបញ្ជាទិញអាចបញ្ចូល',selected:'បានជ្រើស {{n}} · {{sum}} USDT',merged:'បានបញ្ចូល {{n}} ការបញ្ជាទិញ',included:'ការបញ្ជាទិញរួម',needTwo:'សូមជ្រើសយ៉ាងហោចណាស់ 2 ការបញ្ជាទិញ',name:'ឈ្មោះ',phone:'ទូរស័ព្ទ',wechat:'WeChat',whatsapp:'WhatsApp',tg:'TG',memberNo:'លេខសមាជិក',account:'គណនី',joined:'ថ្ងៃចុះឈ្មោះ',lang:'ភាសា',valid:'ការបញ្ជាទិញមានសុពលភាព',spent:'ចំណាយសរុប'}};
const lang=()=>I?.locale==='en'?'en':I?.locale==='km'?'km':'zh',tx=(k,v={})=>{let s=L[lang()][k]||L.zh[k]||k;for(const [a,b] of Object.entries(v))s=s.replace(`{{${a}}}`,String(b));return s};
let user=null,profile=null,orders=[],stats={favorites:0,searches:0,orders:0,downloads:0,materials:0},refreshTimer=0,decorating=false;
const invalid=new Set(['expired','failed','cancelled']);
function effective(o){if(o.status==='pending'&&o.created_at&&Date.now()-new Date(o.created_at).getTime()>86400000)return'expired';return o.status||''}
function fmtDate(v){if(!v)return'—';try{return new Intl.DateTimeFormat(lang()==='zh'?'zh-CN':lang()==='km'?'km-KH':'en-US',{year:'numeric',month:'2-digit',day:'2-digit'}).format(new Date(v))}catch{return String(v)}}
function memberId(raw){const clean=String(raw||'').replace(/[^a-zA-Z0-9]/g,'').toUpperCase();if(!clean)return'—';let hash=2166136261;for(let i=0;i<clean.length;i++){hash^=clean.charCodeAt(i);hash=Math.imul(hash,16777619)}const base=(clean+Math.abs(hash>>>0).toString(36).toUpperCase()).replace(/[^A-Z0-9]/g,'');return('GY'+base).slice(0,15).padEnd(15,'0')}
function valueLen(v){return [...String(v??'')].length}
function fact(label,value){return `<div class="member-v2-fact"><span>${esc(label)}</span><strong>${esc(value||'—')}</strong></div>`}
function openSupport(){try{sessionStorage.setItem('gyx_support_intent','profile-edit')}catch{}location.href='shop.html?support=profile-edit'}
function renderProfile(){const host=document.querySelector('.profile-fold-body')||$('profile');if(!host||!profile||!user)return;document.querySelectorAll('.member-v2-card').forEach((e,i)=>i&&e.remove());let card=host.querySelector('.member-v2-card');if(!card){card=document.createElement('div');card.className='member-v2-card';host.insertBefore(card,host.firstChild)}
const validOrders=orders.filter(o=>!o.hidden_by_user&&!invalid.has(effective(o))).length;
const spent=orders.filter(o=>!o.hidden_by_user&&['paid','delivered'].includes(effective(o))).reduce((a,o)=>a+(Number(o.payable_amount)||0),0);
const facts=[
 [tx('tg'),profile.telegram||'—'],[tx('wechat'),profile.wechat||'—'],[tx('valid'),String(validOrders)],[tx('whatsapp'),profile.whatsapp||'—'],[tx('joined'),fmtDate(profile.created_at||user.created_at)],[tx('spent'),`${spent.toFixed(2)} USDT`],[tx('phone'),profile.phone||'—'],[tx('lang'),profile.locale||'—'],[tx('memberNo'),memberId(user.id)],[tx('account'),user.email||'—']
].sort((a,b)=>valueLen(a[1])-valueLen(b[1]));
const quick=facts.slice(0,4),rest=facts.slice(4);
card.innerHTML=`<div class="member-v2-top"><div class="member-v2-identity"><div class="member-v2-avatar-slot"></div><div class="member-v2-name-row"><strong>${esc(profile.display_name||user.email?.split('@')[0]||'GY')}</strong><button type="button" class="member-v2-avatar-change">${esc(tx('change'))}</button></div><div class="member-v2-level-slot"></div></div><div class="member-v2-quick">${quick.map(x=>fact(x[0],x[1])).join('')}</div></div><div class="member-v2-facts">${rest.map(x=>fact(x[0],x[1])).join('')}</div><div class="member-v2-actions"><button type="button" class="btn btn-secondary btn-small member-v2-edit">${esc(tx('edit'))}</button><div class="member-v2-colors"><b>${esc(tx('color'))}</b><div class="member-v2-color-row"></div></div></div>`;
const avatar=$('profileAvatar');if(avatar)card.querySelector('.member-v2-avatar-slot')?.appendChild(avatar);
const level=$('memberLevelBox');if(level)card.querySelector('.member-v2-level-slot')?.appendChild(level);
card.querySelector('.member-v2-avatar-change')?.addEventListener('click',()=>{const b=$('memberAvatarButton');if(b)b.click();else $('memberAvatarFile')?.click()});
card.querySelector('.member-v2-edit')?.addEventListener('click',openSupport);
const colors={blue:'#1478ff',purple:'#7c4dff',green:'#12a36d',orange:'#f28c28',red:'#e44747',cyan:'#00a6b8'},row=card.querySelector('.member-v2-color-row');for(const [key,color] of Object.entries(colors)){const b=document.createElement('button');b.type='button';b.className='member-v2-color'+(profile.theme_color===key?' active':'');b.style.setProperty('--swatch',color);b.setAttribute('aria-label',key);b.onclick=async()=>{const r=await db.from('profiles').update({theme_color:key}).eq('user_id',user.id);if(r.error)return;profile.theme_color=key;document.documentElement.style.setProperty('--member-accent',color);row.querySelectorAll('button').forEach(x=>x.classList.toggle('active',x===b))};row.appendChild(b)}
}
function foldCount(id,n){const section=$(id);if(!section)return;const head=section.querySelector(':scope > .member-fold-head'),title=head?.querySelector('.member-fold-title');if(!title)return;let badge=title.querySelector('.member-count-badge');if(!badge){badge=document.createElement('em');badge.className='member-count-badge';title.appendChild(badge)}badge.textContent=String(n);const brief=head.querySelector('.member-fold-brief');if(brief)brief.textContent=tx('total',{n})}
function renderCounts(){foldCount('favorites',stats.favorites);foldCount('searches',stats.searches);foldCount('orders',stats.orders);foldCount('downloads',stats.downloads);foldCount('materials',stats.materials)}
async function count(table){const r=await db.from(table).select('id',{count:'exact',head:true}).eq('user_id',user.id);return r.error?0:Number(r.count||0)}
async function refresh(){if(!user)return;const [pr,or,fc,sc,mc]=await Promise.all([
 db.from('profiles').select('display_name,phone,wechat,whatsapp,telegram,locale,created_at,member_level,avatar_url,theme_color').eq('user_id',user.id).maybeSingle(),
 db.from('orders').select('id,order_no,status,payable_amount,currency,hidden_by_user,created_at').eq('user_id',user.id).order('created_at',{ascending:false}).limit(500),
 count('answer_favorites'),count('search_history'),count('member_materials')
]);
if(!pr.error)profile=pr.data||{};if(!or.error)orders=or.data||[];stats={favorites:fc,searches:sc,orders:orders.filter(o=>!o.hidden_by_user).length,downloads:orders.filter(o=>!o.hidden_by_user&&['paid','delivered'].includes(effective(o))).length,materials:mc};renderProfile();renderCounts();setupMerge();decorateOrders()}
function scheduleRefresh(delay=500){clearTimeout(refreshTimer);refreshTimer=setTimeout(refresh,delay)}
function setupMerge(){const section=$('orders'),filters=$('orderFilters');if(!section||!filters)return;let bar=$('memberMergeBar');if(!bar){bar=document.createElement('div');bar.id='memberMergeBar';bar.className='member-merge-bar';bar.innerHTML=`<label><input id="memberMergeAll" type="checkbox"> <span>${esc(tx('selectAll'))}</span></label><span id="memberMergeSelected">${esc(tx('selected',{n:0,sum:'0.00'}))}</span><button id="memberMergeButton" class="btn btn-small" type="button" disabled>${esc(tx('merge'))}</button>`;filters.insertAdjacentElement('afterend',bar);const panel=document.createElement('div');panel.id='memberMergeSummary';panel.className='member-merge-summary';panel.hidden=true;bar.insertAdjacentElement('afterend',panel);$('memberMergeAll').addEventListener('change',e=>{document.querySelectorAll('#orderList .member-merge-check:not(:disabled)').forEach(x=>x.checked=e.target.checked);updateMerge()});$('memberMergeButton').addEventListener('click',mergeSelected)}updateMerge()}
function decorateOrders(){if(decorating)return;decorating=true;try{const map=new Map(orders.map(o=>[String(o.order_no||''),o]));document.querySelectorAll('#orderList>.order-card').forEach(card=>{const no=String(card.querySelector('.order-number')?.textContent||'').trim().split(' ')[0];const o=map.get(no);if(!o)return;let wrap=card.querySelector('.member-merge-pick');const eligible=!o.hidden_by_user&&['pending','checking'].includes(effective(o));if(!wrap){wrap=document.createElement('label');wrap.className='member-merge-pick';wrap.innerHTML=`<input type="checkbox" class="member-merge-check"><span>${esc(tx('merge'))}</span>`;card.insertBefore(wrap,card.firstChild);wrap.querySelector('input').addEventListener('change',updateMerge)}const chk=wrap.querySelector('input');chk.dataset.orderId=o.id;chk.dataset.orderNo=o.order_no||'';chk.dataset.amount=String(Number(o.payable_amount)||0);chk.disabled=!eligible;wrap.hidden=!eligible})}finally{decorating=false}updateMerge()}
function selected(){return [...document.querySelectorAll('#orderList .member-merge-check:checked:not(:disabled)')].map(x=>({id:x.dataset.orderId,no:x.dataset.orderNo,amount:Number(x.dataset.amount)||0}))}
function updateMerge(){const a=selected(),sum=a.reduce((x,o)=>x+o.amount,0),text=$('memberMergeSelected'),btn=$('memberMergeButton');if(text)text.textContent=tx('selected',{n:a.length,sum:sum.toFixed(2)});if(btn)btn.disabled=a.length<2}
function mergeSelected(){const a=selected();if(a.length<2){alert(tx('needTwo'));return}const sum=a.reduce((x,o)=>x+o.amount,0),draft={created_at:new Date().toISOString(),order_ids:a.map(x=>x.id),order_nos:a.map(x=>x.no),total_amount:Number(sum.toFixed(2)),currency:'USDT'};try{localStorage.setItem('gyx_merged_order_draft',JSON.stringify(draft))}catch{}window.GYX_MERGED_ORDER_DRAFT=draft;const p=$('memberMergeSummary');if(p){p.hidden=false;p.innerHTML=`<strong>${esc(tx('merged',{n:a.length}))} · ${sum.toFixed(2)} USDT</strong><span>${esc(tx('included'))}: ${a.map(x=>esc(x.no)).join('、')}</span>`}}
function observe(){['favoriteList','searchHistoryList','orderList','downloadList','materialList'].forEach(id=>{const el=$(id);if(!el)return;new MutationObserver(()=>{if(id==='orderList')decorateOrders();scheduleRefresh(900)}).observe(el,{childList:true,subtree:true})});document.addEventListener('member:opened',()=>scheduleRefresh(150));window.addEventListener('gyx:profile-updated',()=>scheduleRefresh(100));window.addEventListener('gyx:languagechange',()=>scheduleRefresh(100));setInterval(()=>scheduleRefresh(0),30000)}
async function init(){try{user=await window.gyxGetVerifiedUser?.()}catch{}if(!user)return;await refresh();observe();setTimeout(()=>{renderProfile();decorateOrders()},800)}
if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',init,{once:true});else init();
})();