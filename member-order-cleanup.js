(() => {
  'use strict';
  const db = window.gyxSupabase;
  if (!db) return;
  const toast=(text,error=false)=>{const e=document.getElementById('toast');if(!e)return;e.textContent=text;e.className='toast show'+(error?' error':'');clearTimeout(window.__memberQuickToast);window.__memberQuickToast=setTimeout(()=>e.className='toast',1800)};
  let currentUser=null;
  async function user(){if(currentUser)return currentUser;currentUser=await window.gyxGetVerifiedUser?.();return currentUser}
  function cleanQuote(v){return String(v||'').trim().replace(/^[“\"']|[”\"']$/g,'')}
  async function favoriteRow(card){const u=await user();if(!u)return null;const question=cleanQuote(card.querySelector('.favorite-question')?.textContent||'');const title=String(card.querySelector('h3')?.textContent||'').trim();let q=db.from('answer_favorites').select('id,answer_id,question,selections,tier,product_id,quoted_price,matched_title,matched_summary').eq('user_id',u.id);if(question)q=q.eq('question',question);if(title)q=q.eq('matched_title',title);let r=await q.order('updated_at',{ascending:false}).limit(1).maybeSingle();if(!r.data&&question)r=await db.from('answer_favorites').select('id,answer_id,question,selections,tier,product_id,quoted_price,matched_title,matched_summary').eq('user_id',u.id).eq('question',question).order('updated_at',{ascending:false}).limit(1).maybeSingle();return r.data||null}
  async function quickRemoveFavorite(card){if(!confirm('确定取消收藏吗？'))return;const parent=card.parentNode,next=card.nextSibling;card.style.opacity='.45';card.style.pointerEvents='none';requestAnimationFrame(()=>card.remove());try{const row=await favoriteRow(card);const u=await user();if(!row||!u)throw new Error('FAVORITE_NOT_FOUND');const r=await db.from('answer_favorites').delete().eq('id',row.id).eq('user_id',u.id);if(r.error)throw r.error;toast('已取消收藏')}catch{card.style.opacity='';card.style.pointerEvents='';if(parent){if(next)parent.insertBefore(card,next);else parent.appendChild(card)}toast('取消收藏失败，请稍后再试',true)}}
  async function quickOrderFavorite(card,button){const u=await user();if(!u){location.href='login.html?next='+encodeURIComponent('member.html#favorites');return}const old=button.textContent;button.disabled=true;button.textContent='正在打开订单…';try{const row=await favoriteRow(card);if(!row?.product_id||!row?.answer_id)throw new Error('FAVORITE_NOT_FOUND');sessionStorage.setItem('gyx_checkout_request',JSON.stringify({source:'favorite',product_id:row.product_id,match:{question:row.question||'',selections:Array.isArray(row.selections)?row.selections:[],tier:row.tier||'essential',answer:{id:row.answer_id,title:row.matched_title||'',answer_summary:row.matched_summary||''}}}));location.href='shop.html?resume=unified-checkout'}catch{toast('暂时无法打开订单，请稍后再试',true);button.disabled=false;button.textContent=old}}
  document.addEventListener('click',e=>{const button=e.target.closest?.('a,button');if(!button)return;const card=button.closest?.('.favorite-card');if(!card)return;if(button.matches('a[href*="resume=order"],a[href*="favorite="]')){e.preventDefault();e.stopImmediatePropagation();quickOrderFavorite(card,button);return}if(button.classList.contains('btn-danger')&&(button.textContent||'').includes('取消')){e.preventDefault();e.stopImmediatePropagation();quickRemoveFavorite(card)}},true);
  let hiddenOrders=new Map();async function loadHiddenOrders(){const u=await user();if(!u)return;const {data}=await db.from('orders').select('id,order_no,hidden_by_user').eq('user_id',u.id).eq('hidden_by_user',true);hiddenOrders=new Map((data||[]).map(r=>[String(r.order_no||''),Number(r.id)]))}function orderNoFromCard(card){const m=(card.textContent||'').match(/GYX[A-Z0-9]{10,40}/);return m?m[0]:''}async function hideOrder(card,orderNo){if(!confirm('确定删除这条订单记录吗？'))return;const parent=card.parentNode,next=card.nextSibling;card.style.opacity='.45';requestAnimationFrame(()=>card.remove());try{const u=await user();const q=await db.from('orders').select('id').eq('user_id',u.id).eq('order_no',orderNo).maybeSingle();if(!q.data?.id)throw 0;const r=await db.rpc('hide_own_order',{p_order_id:q.data.id});if(r.error||r.data!==true)throw 0;hiddenOrders.set(orderNo,q.data.id);toast('已删除')}catch{card.style.opacity='';if(parent){if(next)parent.insertBefore(card,next);else parent.appendChild(card)}toast('删除失败，请稍后再试',true)}}function processCards(){document.querySelectorAll('#orderList .order-card').forEach(card=>{const orderNo=orderNoFromCard(card);if(!orderNo)return;if(hiddenOrders.has(orderNo)){card.remove();return}if(card.querySelector('[data-member-delete-order]'))return;const actions=document.createElement('div');actions.className='order-actions';const b=document.createElement('button');b.type='button';b.className='btn btn-danger btn-small';b.dataset.memberDeleteOrder='1';b.textContent='删除';b.addEventListener('click',()=>hideOrder(card,orderNo));actions.appendChild(b);card.appendChild(actions)});document.querySelectorAll('#downloadList .order-card').forEach(card=>{const n=orderNoFromCard(card);if(n&&hiddenOrders.has(n))card.remove()})}let scheduled=false;function scheduleCards(){if(scheduled)return;scheduled=true;requestAnimationFrame(()=>{scheduled=false;processCards()})}document.addEventListener('DOMContentLoaded',async()=>{await loadHiddenOrders();scheduleCards();const o=new MutationObserver(scheduleCards);['orderList','downloadList'].forEach(id=>{const el=document.getElementById(id);if(el)o.observe(el,{childList:true})})});
})();