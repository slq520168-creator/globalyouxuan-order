(()=>{'use strict';const db=window.gyxSupabase,I=window.GYXI18N;if(!db||!I)return;let rows=[],answers=new Map(),timer=null,busy=false;const fixedFallback={"automation-trial":['体验版','Trial','សាកល្បង'],"ai-trial":['体验版','Trial','សាកល្បង']};function orderNo(card){return String(card?.querySelector('.order-number')?.textContent||'').trim().split('·')[0].trim().split(' ')[0]}function fixedName(id,fallback){const key=`fixed.${String(id||'')}.name`;const translated=I.t?.(key);if(translated&&translated!==key)return translated;const v=fixedFallback[String(id||'')];if(v)return I.locale==='en'?v[1]:I.locale==='km'?v[2]:v[0];return fallback||id||''}function apply(){busy=true;document.querySelectorAll('#orderList>.order-card,#downloadList>.order-card').forEach(card=>{const row=rows.find(x=>String(x.order_no||'')===orderNo(card));if(!row)return;const title=card.querySelector('.order-title');if(!title)return;const a=answers.get(Number(row.answer_id));title.textContent=a?(I.locale==='en'?(a.title_en||a.title):I.locale==='km'?(a.title_km||a.title):a.title):fixedName(row.product_id,row.matched_answer_title||row.product_name||row.product_id)});busy=false}async function useSnapshot(snapshot){rows=Array.isArray(snapshot)?snapshot:[];const ids=[...new Set(rows.map(x=>Number(x.answer_id)).filter(Boolean))];answers=new Map();if(ids.length){const a=await db.from('product_answer_options').select('id,title,title_en,title_km').in('id',ids);if(!a.error)(a.data||[]).forEach(x=>answers.set(Number(x.id),x))}apply()}function schedule(delay=40){clearTimeout(timer);timer=setTimeout(apply,delay)}function init(){if(Array.isArray(window.GYX_MEMBER_ORDER_SNAPSHOT))useSnapshot(window.GYX_MEMBER_ORDER_SNAPSHOT);window.addEventListener('gyx:orders-snapshot',e=>useSnapshot(e.detail?.orders||[]));window.addEventListener('gyx:languagechange',()=>schedule(0));document.addEventListener('member:opened',e=>{if(e.target?.id==='orders'||e.target?.id==='downloads')schedule()})}if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',init,{once:true});else init()})();