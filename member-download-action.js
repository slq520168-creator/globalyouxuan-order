(()=>{'use strict';if(window.__GYX_DOWNLOAD_ACTION__)return;window.__GYX_DOWNLOAD_ACTION__=1;const db=window.gyxSupabase;if(!db)return;const I=window.GYXI18N;const L=(zh,en,km)=>I?.locale==='en'?en:I?.locale==='km'?km:zh;const orderNo=card=>(card?.textContent||'').match(/GYX[A-Z0-9]{10,40}/)?.[0]||'';async function getOrder(no){const u=await window.gyxGetVerifiedUser?.();if(!u||!no)return null;const {data}=await db.from('orders').select('id,order_no,status,answer_id,matched_answer_title,product_name').eq('user_id',u.id).eq('order_no',no).eq('status','delivered').maybeSingle();return data||null}function safeName(v){return String(v||'GlobalYouXuan').replace(/[\\/:*?"<>|]+/g,'_').slice(0,80)}async function download(card,btn){const no=orderNo(card),o=await getOrder(no);if(!o?.answer_id){btn.textContent=L('暂无下载','Unavailable','មិនមាន');return}const old=btn.textContent;btn.disabled=true;btn.textContent=L('正在准备…','Preparing…','កំពុងរៀបចំ…');try{const r=await window.gyxInvokeFunction('get-purchased-answer',{order_id:o.id,locale:I?.locale||'zh-CN'});const content=r?.answer?.content;if(!content)throw new Error('NO_CONTENT');const blob=new Blob([content],{type:'text/plain;charset=utf-8'}),url=URL.createObjectURL(blob),a=document.createElement('a');a.href=url;a.download=safeName(o.matched_answer_title||o.product_name)+'-'+o.order_no+'.txt';document.body.appendChild(a);a.click();a.remove();setTimeout(()=>URL.revokeObjectURL(url),1500);btn.textContent=L('下载完成','Downloaded','បានទាញយក');btn.dataset.downloadDone='1'}catch(e){console.error('download answer',e);btn.textContent=old;const t=document.getElementById('toast');if(t){t.textContent=L('下载失败，请重试','Download failed. Retry.','ទាញយកបរាជ័យ');t.className='toast show error';setTimeout(()=>t.className='toast',2200)}}finally{btn.disabled=false}}function enhance(){document.querySelectorAll('#downloadList>.order-card').forEach(card=>{if(card.dataset.downloadReady==='1')return;const btn=[...card.querySelectorAll('button.btn')].find(b=>/查看交付|View Delivery|View purchased|មើល/.test(b.textContent||''))||card.querySelector('button.btn');if(!btn)return;card.dataset.downloadReady='1';btn.textContent=L('点击下载','Download','ចុចទាញយក');btn.replaceWith(btn.cloneNode(true));const fresh=card.querySelector('button.btn');fresh.addEventListener('click',e=>{e.preventDefault();e.stopImmediatePropagation();download(card,fresh)},true);card.querySelectorAll('.purchased-answer').forEach(x=>x.remove());card.querySelectorAll('.status-badge').forEach(x=>x.remove())})}function init(){enhance();const list=document.getElementById('downloadList');if(list)new MutationObserver(()=>setTimeout(enhance,20)).observe(list,{childList:true,subtree:true});document.addEventListener('member:opened',e=>{if(e.target?.id==='downloads')setTimeout(enhance,50)});window.addEventListener('gyx:orders-changed',()=>setTimeout(enhance,120))}document.readyState==='loading'?document.addEventListener('DOMContentLoaded',init,{once:true}):init()})();