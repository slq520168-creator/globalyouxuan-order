(()=>{'use strict';let timer=0;function clearAfterOpened(){clearInterval(timer);if((location.hash||'')!=='#downloads')return;let tries=0;timer=setInterval(()=>{tries++;const section=document.getElementById('downloads'),head=section?.querySelector(':scope > .member-fold-head'),body=section?.querySelector(':scope > .member-fold-body');const opened=head?.getAttribute('aria-expanded')==='true'&&body&&!body.hidden;if(opened){clearInterval(timer);timer=0;history.replaceState(history.state,'',location.pathname+location.search);return}if(tries>=100){clearInterval(timer);timer=0}},50)}if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',clearAfterOpened,{once:true});else clearAfterOpened();window.addEventListener('hashchange',clearAfterOpened);window.addEventListener('pagehide',()=>clearInterval(timer),{once:true})})();
