(()=>{'use strict';const I=window.GYXI18N;const L=(zh,en,km)=>I?.locale==='en'?en:I?.locale==='km'?km:zh;if(!document.getElementById('member-compact-layout')){const s=document.createElement('style');s.id='member-compact-layout';s.textContent=`.page-hero{display:none!important}@media(max-width:960px){main{padding-top:0!important}.dashboard-grid{padding-top:10px!important;padding-bottom:160px!important}.dashboard-main{gap:14px!important;padding-bottom:40px!important}.panel{margin-bottom:0!important}#orders,#downloads,#materials{scroll-margin-bottom:120px!important}.order-list{padding-bottom:36px!important}}`;document.head.appendChild(s)}const seenFailed=new WeakSet(),seenChecking=new WeakSet();function enhanceChecking(){document.querySelectorAll('#orderList .order-card').forEach(card=>{if(seenChecking.has(card)||!card.querySelector('.status-badge.checking'))return;seenChecking.add(card);const row=card.querySelector('.txid-row');if(row){const input=row.querySelector('input'),button=row.querySelector('button');if(input){input.readOnly=true;input.setAttribute('aria-readonly','true')}if(button)button.remove()}const tip=document.createElement('p');tip.className='order-number';tip.textContent=L('付款信息已提交，正在核验中，请勿重复提交。','Payment submitted and under verification. Please do not submit again.','ព័ត៌មានការបង់ប្រាក់បានដាក់ស្នើ ហើយកំពុងផ្ទៀងផ្ទាត់។ សូមកុំដាក់ស្នើម្តងទៀត។');card.appendChild(tip)})}function enhanceFailed(){document.querySelectorAll('#orderList .order-card').forEach(card=>{if(seenFailed.has(card)||!card.querySelector('.status-badge.failed'))return;const orderNo=(card.querySelector('.order-number')?.textContent||'').trim().split('·')[0].trim();if(!/^GYX[A-Z0-9]{10,40}$/.test(orderNo))return;seenFailed.add(card);const tip=document.createElement('p');tip.className='order-number';tip.textContent=L('付款核验失败，可重新提交新的 TXID。','Payment verification failed. You can submit a new TXID.','ការផ្ទៀងផ្ទាត់ការបង់ប្រាក់បានបរាជ័យ។ អ្នកអាចដាក់ TXID ថ្មី។');const row=document.createElement('div');row.className='txid-row';const input=document.createElement('input');input.className='field';input.type='text';input.maxLength=64;input.autocapitalize='characters';input.autocomplete='off';input.placeholder=L('重新粘贴64位TRON交易哈希','Paste a new 64-character TRON transaction hash','បិទភ្ជាប់ TRON TXID ថ្មី 64 តួ');const button=document.createElement('button');button.className='btn';button.type='button';button.textContent=L('重新提交付款','Resubmit payment','ដាក់ការបង់ប្រាក់ម្តងទៀត');button.addEventListener('click',async()=>{const txid=input.value.trim().toUpperCase();if(!/^[0-9A-F]{64}$/.test(txid)){alert(L('请输入有效的64位 TRON TXID','Enter a valid 64-character TRON TXID','សូមបញ្ចូល TRON TXID 64 តួដែលត្រឹមត្រូវ'));return}button.disabled=true;button.textContent=L('正在提交…','Submitting…','កំពុងដាក់ស្នើ…');try{const r=await window.gyxInvokeFunction('submit-payment',{order_no:orderNo,txid});alert(r&&['paid','delivered'].includes(r.status)?L('付款已确认。','Payment confirmed.','ការបង់ប្រាក់ត្រូវបានបញ្ជាក់។'):L('新的 TXID 已提交，正在核验。','New TXID submitted and under verification.','TXID ថ្មីត្រូវបានដាក់ស្នើ ហើយកំពុងផ្ទៀងផ្ទាត់។'));document.getElementById('refreshOrdersButton')?.click()}catch(e){alert(L('重新提交失败，请检查 TXID 后重试。','Resubmission failed. Check the TXID and try again.','ការដាក់ស្នើឡើងវិញបរាជ័យ។ សូមពិនិត្យ TXID ហើយសាកល្បងម្តងទៀត។'));button.disabled=false;button.textContent=L('重新提交付款','Resubmit payment','ដាក់ការបង់ប្រាក់ម្តងទៀត')}});row.append(input,button);card.append(tip,row)})}function enhance(){enhanceChecking();enhanceFailed()}function runSoon(){setTimeout(enhance,120)}function init(){runSoon();document.addEventListener('member:opened',e=>{if(e.target?.id==='orders')runSoon()});document.getElementById('refreshOrdersButton')?.addEventListener('click',runSoon);window.addEventListener('gyx:orders-changed',runSoon);window.addEventListener('gyx:languagechange',runSoon)}if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',init,{once:true});else init()})();