(()=>{'use strict';const db=window.gyxSupabase,I=window.GYXI18N;if(!db)return;const $=id=>document.getElementById(id);let user=null,favorites=[];const toast=(s,e=false)=>{const x=$('toast');if(!x)return;x.textContent=s;x.className=`toast show${e?' error':''}`;clearTimeout(toast.t);toast.t=setTimeout(()=>x.className='toast',2200)};async function merged(body){const{data}=await db.auth.getSession(),token=data?.session?.access_token;if(!token)throw Error('请重新登录');const r=await fetch('https://afzcohtnljnmucrkgcaz.supabase.co/functions/v1/create_merged_order',{method:'POST',headers:{'content-type':'application/json',authorization:'Bearer '+token},body:JSON.stringify(body)}),j=await r.json().catch(()=>({}));if(!r.ok||j.error)throw Error(j.error||'下单失败');showPay(j.order)}let current=null;function payModal(){let m=$('memberMergedPayModal');if(m)return m;m=document.createElement('div');m.id='memberMergedPayModal';m.className='modal';m.innerHTML='<div class="modal-card"><div class="modal-head"><div><h2>合并订单</h2><p>已按当前勾选内容创建一个订单</p></div><button class="icon-btn" data-mclose>✕</button></div><div class="order-summary"><div class="summary-row"><span>订单号</span><strong id="mPayNo">—</strong></div><div class="summary-row"><span>应付金额</span><strong><span id="mPayAmount">—</span> USDT</strong></div><div class="summary-row"><span>网络</span><strong id="mPayNetwork">TRC20</strong></div></div><div class="form-group"><label>收款地址</label><div class="wallet-box"><span id="mPayWallet">—</span><button id="mCopyWallet" class="btn btn-small" type="button">复制</button></div></div><form id="mPayForm" class="form-stack"><div class="form-group"><label>交易哈希 TXID</label><input id="mPayTx" class="field" maxlength="64"></div><div id="mPayMsg" class="form-message"></div><div class="modal-actions"><button class="btn btn-secondary" data-mclose type="button">关闭</button><button id="mPaySubmit" class="btn" type="submit">提交并核验付款</button></div></form></div>';document.body.appendChild(m);m.querySelectorAll('[data-mclose]').forEach(b=>b.onclick=()=>{m.classList.remove('show','open');document.body.classList.remove('modal-open');document.body.style.overflow=''});$('mCopyWallet').onclick=async()=>{const v=$('mPayWallet').textContent.trim();try{await navigator.clipboard.writeText(v);toast('收款地址已复制')}catch{toast(v)}};$('mPayForm').onsubmit=submitPay;return m}function showPay(o){current=o;const m=payModal();$('mPayNo').textContent=o.order_no;$('mPayAmount').textContent=Number(o.payable_amount||0).toFixed(2);$('mPayNetwork').textContent=o.network||'TRC20';$('mPayWallet').textContent=o.wallet_address||'';$('mPayTx').value='';$('mPayMsg').textContent='';m.classList.add('show','open');document.body.classList.add('modal-open');document.body.style.overflow='hidden';window.dispatchEvent(new CustomEvent('gyx:orders-changed'))}async function submitPay(e){e.preventDefault();const tx=$('mPayTx').value.trim().toUpperCase(),b=$('mPaySubmit'),msg=$('mPayMsg');if(!current||!/^[0-9A-F]{64}$/.test(tx)){msg.textContent='请输入正确的64位TRON交易哈希';msg.className='form-message show error';return}b.disabled=true;try{const r=await window.gyxInvokeFunction('submit-payment',{order_id:current.id,order_no:current.order_no,txid:tx});msg.textContent=['paid','delivered'].includes(r?.status||'')?'付款已确认':'链上仍在确认，请稍后查看订单';msg.className='form-message show success';window.dispatchEvent(new CustomEvent('gyx:orders-changed'))}catch{msg.textContent='付款核验暂时未完成';msg.className='form-message show error'}finally{b.disabled=false}}async function go(body){try{toast('正在创建合并订单…');await merged(body)}catch(e){console.error(e);toast('一键下单失败：'+e.message,true)}}const searchIds=()=>[...document.querySelectorAll('#searchHistoryList .member-search-check:checked')].map(x=>x.closest('article')?.dataset.historyId).filter(Boolean),orderIds=()=>[...document.querySelectorAll('#orderList .member-merge-check:checked:not(:disabled)')].map(x=>Number(x.dataset.orderId)).filter(Number.isFinite),favoriteIds=()=>[...document.querySelectorAll('#favoriteList .member-favorite-check:checked')].map(x=>Number(x.dataset.favoriteId)).filter(Number.isFinite);function setupSearch(){const merge=$('memberSearchMerge'),del=$('memberSearchDeleteSelected'),all=$('memberSearchSelectAll');if(!merge)return;if(!merge.dataset.unified){merge.dataset.unified='1';merge.textContent='一键下单';merge.addEventListener('click',e=>{e.preventDefault();e.stopImmediatePropagation();const ids=searchIds();ids.length?go({search_ids:ids}):toast('请先勾选搜索记录')},true)}if(del&&del.textContent!=='一键删除')del.textContent='一键删除';if(all&&!all.dataset.auto){all.dataset.auto='1';all.checked=true;document.querySelectorAll('#searchHistoryList .member-search-check').forEach(x=>x.checked=true);all.dispatchEvent(new Event('change',{bubbles:true}))}}async function deleteOrders(){const ids=orderIds();if(!ids.length)return toast('请先勾选订单');const r=await db.from('orders').select('id,status').eq('user_id',user.id).in('id',ids),ok=(r.data||[]).filter(x=>!['paid','delivered'].includes(String(x.status))).map(x=>x.id);if(!ok.length)return toast('成功订单不能删除',true);if(!confirm(`确定删除勾选的 ${ok.length} 条未成功订单吗？`))return;let n=0;for(const id of ok){const x=await db.rpc('hide_own_order',{p_order_id:id});if(!x.error&&x.data===true)n++}toast(`已删除 ${n} 条订单`);location.reload()}function setupOrders(){const bar=$('memberMergeBar'),merge=$('memberMergeButton'),all=$('memberMergeAll');if(!bar||!merge)return;if(!merge.dataset.unified){merge.dataset.unified='1';merge.textContent='一键下单';merge.addEventListener('click',e=>{e.preventDefault();e.stopImmediatePropagation();const ids=orderIds();ids.length?go({order_ids:ids}):toast('请先勾选订单')},true)}merge.disabled=false;if(all&&!all.dataset.auto){all.dataset.auto='1';all.checked=true;document.querySelectorAll('#orderList .member-merge-check:not(:disabled)').forEach(x=>x.checked=true);all.dispatchEvent(new Event('change',{bubbles:true}))}if(!$('memberSelectedDeleteOrders')){const d=document.createElement('button');d.id='memberSelectedDeleteOrders';d.className='btn btn-danger btn-small';d.type='button';d.textContent='一键删除';d.onclick=deleteOrders;bar.appendChild(d)}$('memberBulkDeleteOrders')?.remove()}async function loadFav(){const r=await db.from('answer_favorites').select('id,updated_at').eq('user_id',user.id).order('updated_at',{ascending:false});if(!r.error)favorites=r.data||[];setupFav()}function setupFav(){const list=$('favoriteList'),cards=[...(list?.querySelectorAll(':scope>.favorite-card')||[])];if(!cards.length)return;cards.forEach((c,i)=>{const row=favorites[i];if(!row)return;let p=c.querySelector('.member-favorite-pick');if(!p){p=document.createElement('label');p.className='member-favorite-pick';p.innerHTML='<input class="member-favorite-check" type="checkbox" checked><span>选择</span>';c.insertBefore(p,c.firstChild)}p.querySelector('input').dataset.favoriteId=row.id});let bar=$('memberFavoriteBulk');if(!bar){bar=document.createElement('div');bar.id='memberFavoriteBulk';bar.className='member-favorite-bulk';bar.innerHTML='<label><input id="memberFavoriteAll" type="checkbox" checked><span>全选收藏</span></label><span id="memberFavoriteBulkCount"></span><button id="memberFavoriteOrder" class="btn btn-small">一键下单</button><button id="memberFavoriteDelete" class="btn btn-danger btn-small">一键取消</button>';list.parentElement.insertBefore(bar,list);$('memberFavoriteAll').onchange=e=>{list.querySelectorAll('.member-favorite-check').forEach(x=>x.checked=e.target.checked);favCount()};list.addEventListener('change',e=>{if(e.target.matches('.member-favorite-check'))favCount()});$('memberFavoriteOrder').onclick=()=>{const ids=favoriteIds();ids.length?go({favorite_ids:ids}):toast('请先勾选收藏')};$('memberFavoriteDelete').onclick=async()=>{const ids=favoriteIds();if(!ids.length)return toast('请先勾选收藏');if(!confirm(`确定取消 ${ids.length} 条收藏吗？`))return;const r=await db.from('answer_favorites').delete().eq('user_id',user.id).in('id',ids).select('id');if(r.error)return toast('取消收藏失败',true);toast(`已取消 ${r.data?.length||0} 条收藏`);location.reload()}}favCount()}function favCount(){const n=favoriteIds().length;if($('memberFavoriteBulkCount'))$('memberFavoriteBulkCount').textContent=`已选 ${n} 条`}async function init(){try{user=await window.gyxGetVerifiedUser?.()}catch{}if(!user)return;await loadFav();const tick=()=>{setupSearch();setupOrders();setupFav()};tick();['favoriteList','searchHistoryList','orderList'].forEach(id=>{const e=$(id);if(e)new MutationObserver(()=>setTimeout(tick,0)).observe(e,{childList:true})});setTimeout(tick,1000)}if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',init,{once:true});else init()})();