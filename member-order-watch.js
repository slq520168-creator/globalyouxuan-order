(()=>{'use strict';if(window.__GYX_ORDER_WATCH__)return;window.__GYX_ORDER_WATCH__=1;let timer=0;const ACTIVE=new Set(['checking','paid']);const hasActive=orders=>Array.isArray(orders)&&orders.some(o=>ACTIVE.has(String(o?.status||'')));function stop(){if(timer){clearInterval(timer);timer=0}}function tick(){if(document.hidden){stop();return}window.dispatchEvent(new CustomEvent('gyx:orders-changed',{detail:{source:'order-watch'}}))}function evaluate(orders){if(document.hidden||!hasActive(orders)){stop();return}if(timer)return;timer=setInterval(tick,5000)}window.addEventListener('gyx:orders-snapshot',e=>evaluate(e.detail?.orders));document.addEventListener('visibilitychange',()=>{if(document.hidden){stop();return}evaluate(window.GYX_MEMBER_ORDER_SNAPSHOT)});window.addEventListener('pagehide',stop,{once:true});evaluate(window.GYX_MEMBER_ORDER_SNAPSHOT)})();