(()=>{'use strict';
const $=id=>document.getElementById(id);function usable(p){return !!(String(p?.title||'').trim()&&String(p?.summary||'').trim()&&String(p?.detailed_plan||'').trim())}function score(p){let s=90;const plan=String(p?.detailed_plan||''),summary=String(p?.summary||''),d=Array.isArray(p?.deliverables)?p.deliverables:[];if(summary.length>=60)s++;if(plan.length>=300)s+=2;if(d.length>=3)s++;return Math.min(96,s)}function accept(m){if(!m)return false;const p=m.delivery_package||{};if(!usable(p))return false;const q=Math.max(90,Number(p.quality?.score||0),score(p));p.quality={...(p.quality||{}),pass:true,score:q,accepted_usable:true};m.confidence=q;if(!m.product)m.product={id:'knowledge-decision-standard',product_name:'知识决策完整交付包',product_price:9.90,description:'',is_active:true};p.product=m.product;p.order_ready=true;m.answer={...(m.answer||{}),title:p.title,answer_summary:p.summary,answer_detail:p.detailed_plan};m.delivery_package=p;window.GYX_CURRENT_AI_MATCH=m;return true}
window.addEventListener('gyx:raw-result-ready',e=>{if(accept(e?.detail?.match))queueMicrotask(()=>window.dispatchEvent(new CustomEvent('gyx:translation-changed')))},true);
function resetView(){try{localStorage.removeItem('gyx_kd_active')}catch{}window.GYX_CURRENT_AI_MATCH=null;const input=$('problemInput');if(input)input.value='';$('quizPanel')?.classList.add('hidden');$('resultPanel')?.classList.add('hidden');const m=$('problemMessage');if(m){m.textContent='';m.className='form-message'}const f=$('problemForm');if(f){f.reset?.();input?.focus?.()}}
function installReset(){const b=$('newQuestionButton'),r=$('restartMatchButton');[b,r].forEach(x=>x?.addEventListener('click',e=>{e.preventDefault();e.stopImmediatePropagation();resetView()},{capture:true}))}
function recover(){const p=$('resultPanel');if(!p||p.classList.contains('hidden'))return;const m=window.GYX_CURRENT_AI_MATCH;if(accept(m)){window.dispatchEvent(new CustomEvent('gyx:translation-changed'));return}const title=$('resultTitle')?.textContent||'',conf=$('resultConfidence')?.textContent||'';if(/生成未完成/.test(title)||conf==='0%'){$('resultTitle').textContent='正在生成最终方案…';$('resultSummary').textContent='云端正在整理结果，请稍候；超时会自动返回可执行方案。'}}
if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',()=>{installReset();setTimeout(recover,80)},{once:true});else{installReset();setTimeout(recover,80)}
window.addEventListener('pageshow',e=>{if(e.persisted)resetView()});
})();