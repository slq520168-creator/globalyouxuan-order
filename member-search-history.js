(()=>{'use strict';const $=id=>document.getElementById(id);const SEARCH_PRODUCTS={essential:'answer-essential',standard:'answer-standard',professional:'answer-professional',custom:'answer-custom'};let deleting=new Set();const esc=v=>String(v||'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));function empty(list,text='暂无搜索记录'){list.replaceChildren();const d=document.createElement('div');d.className='empty-state';d.textContent=text;list.appendChild(d)}function tierKey(r){if(SEARCH_PRODUCTS[r?.tier])return r.tier;const c=Number(r?.confidence||0);return c>=93?'custom':c>=85?'professional':c>=75?'standard':'essential'}async function init(){const list=$('searchHistoryList');if(!list||list.dataset.gyxHistoryReady==='1')return;list.dataset.gyxHistoryReady='1';const db=window.gyxSupabase;if(!db){empty(list,'暂时无法读取搜索记录');return}let u;try{u=await window.gyxGetVerifiedUser?.()}catch{}if(!u){empty(list,'登录后可查看搜索记录');return}async function load(){const {data,error}=await db.from('search_history').select('id,question,selections,matched_answer_id,matched_title,matched_summary,tier,confidence,created_at').eq('user_id',u.id).order('created_at',{ascending:false}).limit(100);if(error){empty(list,'搜索记录读取失败');return}if(!data?.length){empty(list,'暂无搜索记录。完成一次5轮匹配后会自动保存在这里。');return}const ids=[...new Set(data.map(r=>Number(r.matched_answer_id)).filter(Number.isFinite))];const answerMap=new Map();if(ids.length){const ar=await db.from('product_answer_options').select('id,title,answer_summary').in('id',ids);for(const a of ar.data||[])answerMap.set(Number(a.id),a)}list.replaceChildren();for(const r of data){const card=document.createElement('article');card.className='favorite-card search-history-card';card.dataset.historyId=String(r.id);const time=new Date(r.created_at).toLocaleString();const sels=Array.isArray(r.selections)?r.selections.filter(Boolean):[];const answer=answerMap.get(Number(r.matched_answer_id))||null;const matched=String(r.matched_title||answer?.title||'私人方案').trim();const summary=String(r.matched_summary||answer?.answer_summary||'').trim();const path=sels.length?`<details class="search-path"><summary>查看5轮选择</summary><ol>${sels.map(x=>`<li>${esc(x)}</li>`).join('')}</ol></details>`:'';const canOrder=Number.isFinite(Number(r.matched_answer_id));const controls=`<div class="search-history-controls">${canOrder?`<button type="button" class="history-order-btn" data-order-history="${esc(r.id)}">下单</button>`:''}<button type="button" class="history-delete-btn" data-del-history="${esc(r.id)}">删除</button></div>`;card.innerHTML=`<div class="favorite-card-head search-history-head"><div><strong>${esc(r.question)}</strong><p class="muted">${esc(time)} · 匹配度 ${Number(r.confidence||0)}%</p></div>${controls}</div><div class="search-final"><span class="muted">当时匹配方案</span><h3>${esc(matched)}</h3>${summary?`<p class="muted">${esc(summary)}</p>`:''}</div>${path}`;card._history={...r,answer,matched};list.appendChild(card)}}list.addEventListener('click',async e=>{const orderBtn=e.target.closest?.('[data-order-history]');if(orderBtn){e.preventDefault();e.stopPropagation();const card=orderBtn.closest('article');const h=card?._history;if(!h||!Number.isFinite(Number(h.matched_answer_id)))return;const tier=tierKey(h),productId=SEARCH_PRODUCTS[tier];const match={question:h.question||'',selections:Array.isArray(h.selections)?h.selections:[],tier,confidence:Number(h.confidence||0),answer:{id:Number(h.matched_answer_id),title:h.matched,answer_summary:h.matched_summary||h.answer?.answer_summary||'',keywords:[]}};orderBtn.disabled=true;const old=orderBtn.textContent;orderBtn.textContent='打开中…';try{await window.GYX_MEMBER_CHECKOUT?.open(productId,match)}finally{if(orderBtn.isConnected){orderBtn.disabled=false;orderBtn.textContent=old}}return}const b=e.target.closest?.('[data-del-history]');if(!b)return;e.preventDefault();e.stopPropagation();const id=String(b.dataset.delHistory||'');if(!id||deleting.has(id))return;if(!window.confirm('确定删除这条搜索记录吗？'))return;deleting.add(id);const card=b.closest('article'),parent=card?.parentNode,next=card?.nextSibling;const old=b.textContent;b.disabled=true;b.textContent='删除中…';if(card){card.style.opacity='.45';card.style.pointerEvents='none';requestAnimationFrame(()=>card.remove())}try{const {error}=await db.from('search_history').delete().eq('id',id).eq('user_id',u.id);if(error)throw error;if(!list.querySelector('[data-history-id]'))empty(list)}catch(err){console.error('delete search history failed',err);if(card&&parent){card.style.opacity='';card.style.pointerEvents='';if(next&&next.parentNode===parent)parent.insertBefore(card,next);else parent.appendChild(card);b.disabled=false;b.textContent=old}}finally{deleting.delete(id)}},false);await load()}if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',init,{once:true});else init()})();