(()=>{
'use strict';
function q(s,r=document){return r.querySelector(s)}
function makeChevron(){const s=document.createElement('span');s.className='member-fold-chevron';s.innerHTML='<b>展开</b><i>⌄</i>';return s}
function updateMarker(header,open){const m=q('.member-fold-chevron',header);if(!m)return;const b=q('b',m);if(b)b.textContent=open?'收起':'展开';header.closest('.panel,.profile-card')?.classList.toggle('member-fold-open',open)}
function toggle(header,body,force){const open=typeof force==='boolean'?force:body.hidden;body.hidden=!open;header.setAttribute('aria-expanded',String(open));updateMarker(header,open);if(open)body.dispatchEvent(new CustomEvent('member:opened',{bubbles:true}))}
function setupProfile(){const card=q('.profile-card');if(!card||card.dataset.foldReady==='1')return;card.dataset.foldReady='1';const children=[...card.children],body=document.createElement('div');body.className='member-fold-body profile-fold-body';children.forEach(n=>body.appendChild(n));const header=document.createElement('button');header.type='button';header.className='member-fold-head profile-fold-head';header.setAttribute('aria-expanded','false');const title=document.createElement('span');title.className='member-fold-title';title.textContent='我的资料';const brief=document.createElement('span');brief.className='member-fold-brief';brief.textContent='账号、会员编号、联系方式';const left=document.createElement('span');left.className='member-fold-labels';left.append(title,brief);header.append(left,makeChevron());card.append(header,body);body.hidden=true;const form=q('#profileForm',body);if(form){form.classList.add('member-profile-edit');form.hidden=true;const edit=document.createElement('button');edit.type='button';edit.className='btn btn-secondary btn-small member-edit-toggle';edit.textContent='修改资料';edit.setAttribute('aria-expanded','false');const meta=q('.profile-meta',body);(meta||q('.profile-name',body)||body).insertAdjacentElement('afterend',edit);edit.addEventListener('click',e=>{e.stopPropagation();const open=form.hidden;form.hidden=!open;edit.setAttribute('aria-expanded',String(open));edit.textContent=open?'收起修改':'修改资料';if(open)setTimeout(()=>q('#profileName')?.focus(),80)})}header.addEventListener('click',()=>toggle(header,body))}
const sectionCopy={favorites:{title:'我的收藏',brief:'收藏的方案，可继续查看或取消收藏'},searches:{title:'我的搜索',brief:'5轮匹配完成后自动保存，可随时回来查看'},orders:{title:'我的订单',brief:'已完成订单 · 待付款订单 · 失效订单'},downloads:{title:'我的下载',brief:'已付款或已完成订单的交付内容'},materials:{title:'我的资料',brief:'个人保存的资料与长文本'},cloudService:{title:'我的云服务',brief:'云端智能服务'},smartSupport:{title:'智能客服',brief:'Telegram客服协助处理'},inbox:{title:'站内消息',brief:'付款核验异常、订单处理提醒'},memberInbox:{title:'站内消息',brief:'付款核验异常、订单处理提醒'}};
function sectionTitle(section){return sectionCopy[section.id]?.title||'会员内容'}function sectionBrief(section){return sectionCopy[section.id]?.brief||'点击展开查看'}
function setupSection(section){if(!section||section.dataset.foldReady==='1')return;section.dataset.foldReady='1';const children=[...section.children],body=document.createElement('div');body.className='member-fold-body';children.forEach(n=>body.appendChild(n));const header=document.createElement('button');header.type='button';header.className='member-fold-head';header.setAttribute('aria-expanded','false');const left=document.createElement('span');left.className='member-fold-labels';const title=document.createElement('span');title.className='member-fold-title';title.textContent=sectionTitle(section);const brief=document.createElement('span');brief.className='member-fold-brief';brief.textContent=sectionBrief(section);left.append(title,brief);header.append(left,makeChevron());section.append(header,body);body.hidden=true;header.addEventListener('click',()=>toggle(header,body))}
async function visitorCountry(){try{const r=await fetch('/cdn-cgi/trace?'+Date.now(),{cache:'no-store',credentials:'same-origin'});if(!r.ok)return'';const t=await r.text();const m=t.match(/(?:^|\n)loc=([A-Z]{2})(?:\n|$)/i);return m?m[1].toUpperCase():''}catch{return''}}
async function ensureCloudSection(){const host=q('.dashboard-main'),materials=q('#materials');if(!host||q('#cloudService'))return false;const country=await visitorCountry();if(!country||country==='CN'){document.documentElement.dataset.cloudRegion=country==='CN'?'blocked-cn':'blocked-unknown';return false}document.documentElement.dataset.cloudRegion=country;const section=document.createElement('section');section.id='cloudService';section.className='panel cloud-service-panel';section.innerHTML='<div class="panel-head"><div><h2>我的云服务</h2><p class="muted">直接输入你的问题或需求</p></div></div><div id="cloudChatList" class="cloud-chat-list"></div><div class="cloud-chat-toolbar"><span id="cloudUsageText" hidden></span><button id="cloudNewSession" class="btn btn-secondary btn-small" type="button">新会话</button></div><form id="cloudChatForm" class="cloud-chat-form"><textarea id="cloudChatInput" class="textarea" rows="2" placeholder="说出你现在想解决的问题…"></textarea><button id="cloudChatSend" class="btn" type="submit">发送</button></form>';if(materials)materials.insertAdjacentElement('afterend',section);else host.appendChild(section);setupSection(section);const sc=document.createElement('script');sc.src='member-cloud-service.js?v=20260809-5';sc.defer=true;document.body.appendChild(sc);return true}
function placeInboxLast(){const host=q('.dashboard-main'),inbox=q('#memberInbox')||q('#inbox');if(host&&inbox&&host.lastElementChild!==inbox)host.appendChild(inbox);if(inbox)setupSection(inbox)}
function watchInbox(){const host=q('.dashboard-main');if(!host)return;placeInboxLast();new MutationObserver(()=>placeInboxLast()).observe(host,{childList:true})}
function openHash(){const id=(location.hash||'').replace('#','');if(!id)return;const section=document.getElementById(id);if(!section)return;setupSection(section);const head=q(':scope > .member-fold-head',section),body=q(':scope > .member-fold-body',section);if(head&&body){toggle(head,body,true);setTimeout(()=>section.scrollIntoView({behavior:'smooth',block:'start'}),60)}}
function wireOverview(){const overview=q('#memberOverview');if(overview)overview.style.display='none'}function cleanHero(){const shortcuts=q('.member-shortcuts');if(shortcuts)shortcuts.style.display='none';const hero=q('.page-hero');if(hero)hero.classList.add('member-hero-compact')}
async function init(){cleanHero();setupProfile();['profile','favorites','searches','orders','downloads','materials','smartSupport','inbox'].forEach(id=>setupSection(document.getElementById(id)));wireOverview();watchInbox();await ensureCloudSection();openHash();window.addEventListener('hashchange',openHash)}
if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',init,{once:true});else init();
})();