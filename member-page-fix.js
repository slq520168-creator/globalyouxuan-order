(()=>{'use strict';function shortMemberId(){const el=document.getElementById('profileUserId');if(!el)return;const raw=String(el.textContent||'').trim();if(!raw||raw==='—')return;const clean=raw.replace(/[^a-zA-Z0-9]/g,'').toUpperCase();const id=('GY'+clean).slice(0,15).padEnd(15,'0');if(el.textContent!==id){el.textContent=id;el.title='会员展示编号'}}function feedback(){document.addEventListener('click',e=>{const b=e.target.closest?.('#favorites .btn-danger,#orderList [data-member-delete-order],#materials .btn-danger');if(!b)return;b.style.opacity='.68';setTimeout(()=>{if(b?.isConnected)b.style.opacity=''},180)},true)}function compact(){document.querySelectorAll('.dashboard-main>.panel').forEach(p=>{p.style.minHeight='0'});shortMemberId()}function init(){compact();feedback();const id=document.getElementById('profileUserId');if(id)new MutationObserver(shortMemberId).observe(id,{childList:true,characterData:true,subtree:true});window.addEventListener('pageshow',compact,{passive:true})}if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',init,{once:true});else init()})();