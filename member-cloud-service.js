(()=>{"use strict";const db=window.gyxSupabase,I=window.GYXI18N,$=id=>document.getElementById(id);if(!db||!I)return;let sessionId="",sending=false;const lang=()=>I.locale||"zh";function makeSession(){sessionId=`cloud_${Date.now()}_${Math.random().toString(36).slice(2,10)}`;try{sessionStorage.setItem("gyx_cloud_session",sessionId)}catch{}}function ensureSession(){if(sessionId)return;try{sessionId=sessionStorage.getItem("gyx_cloud_session")||""}catch{}if(!sessionId)makeSession()}function add(role,text){const list=$("cloudChatList");if(!list)return;const box=document.createElement("div");box.className=`cloud-msg ${role}`;const r=document.createElement("div");r.className="cloud-msg-role";r.textContent=role==="user"?(lang()==="zh"?"我":"You"):(lang()==="zh"?"云服务":"AI");const b=document.createElement("div");b.className="cloud-msg-body";b.textContent=text;box.append(r,b);list.appendChild(box);list.scrollTop=list.scrollHeight;return box}function setBusy(v){sending=v;const b=$("cloudChatSend"),input=$("cloudChatInput");if(b)b.disabled=v;if(input)input.disabled=v}async function send(message){ensureSession();const timer=new Promise((_,reject)=>setTimeout(()=>reject(new Error("CLOUD_TIMEOUT")),25000)),call=window.gyxInvokeFunction("member-cloud-chat",{message,session_id:sessionId,lang:lang()});return await Promise.race([call,timer])}async function submit(e){e.preventDefault();if(sending)return;const input=$("cloudChatInput"),message=input?.value.trim();if(!message)return;input.value="";add("user",message);const loading=add("assistant",lang()==="zh"?"正在回复…":"Replying…");setBusy(true);try{const r=await send(message);loading?.remove();if(r?.error)throw new Error(r.message||r.error);add("assistant",r?.reply||r?.message||(lang()==="zh"?"暂时没有返回内容，请重试。":"No response. Please retry."))}catch(err){console.error("cloud chat failed",err);loading?.remove();add("assistant",err?.message&&err.message!=="CLOUD_TIMEOUT"?err.message:(lang()==="zh"?"云服务连接超时，请重新发送。":"Cloud service timed out. Please retry."))}finally{setBusy(false);input?.focus()}}function init(){const form=$("cloudChatForm"),fresh=$("cloudNewSession");if(!form)return;ensureSession();form.addEventListener("submit",submit);fresh?.addEventListener("click",()=>{makeSession();$("cloudChatList")?.replaceChildren();$("cloudChatInput")?.focus()})}if(document.readyState==="loading")document.addEventListener("DOMContentLoaded",init,{once:true});else init()})();