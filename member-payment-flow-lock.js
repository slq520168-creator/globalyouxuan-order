(()=>{'use strict';const db=window.gyxSupabase;if(!db)return;const $=s=>document.querySelector(s);const locale=()=>String(window.GYXI18N?.locale||localStorage.getItem('lang')||'zh').toLowerCase();const L=(zh,en,km)=>{const l=locale();return l.startsWith('en')?en:l.startsWith('km')?km:zh};let syncing=false;function setCount(n){const sec=$('#downloads');if(!sec)return;const badge=sec.querySelector(':scope > .member-fold-head .member-count-badge');const brief=sec.querySelector(':scope > .member-fold-head .member-fold-brief');if(badge)badge.textContent=String(n);if(brief)brief.textContent=L(`共${n}条`,`${n} records`,`${n} កំណត់ត្រា`)}async function syncDownloads(){if(syncing)return;syncing=true;try{const user=await window.gyxGetVerifiedUser?.();if(!user)return;const {data,error}=await db.from('orders').select('id,order_no,status').eq('user_id',user.id).eq('status','delivered').order('updated_at',{ascending:false});if(error)return;const delivered=new Set((data||[]).map(x=>String(x.order_no||'')));const list=$('#downloadList');if(!list)return;list.querySelectorAll('.order-card').forEach(card=>{const no=(card.textContent||'').match(/GYX[A-Z0-9]{10,40}/)?.[0]||'';if(!delivered.has(no))card.remove()});const cards=list.querySelectorAll('.order-card');if(!cards.length){list.innerHTML='<div class="empty-state">'+L('暂无可下载内容','No downloads available','មិនទាន់មានឯកសារទាញយក')+'</div>'}const lead=$('#downloads .panel-head .muted');if(lead)lead.remove();setCount(delivered.size)}finally{syncing=false}}function fixReturnOrders(){document.addEventListener('click',e=>{const a=e.target.closest?.('#memberPaymentScreen a.action');if(!a)return;const text=(a.textContent||'').trim();if(!/返回我的订单|Back to my orders|ត្រឡប់ទៅការបញ្ជាទិញ/.test(text))return;e.preventDefault();e.stopImmediatePropagation();location.replace('member.html#orders')},true)}function watch(){const list=$('#downloadList');if(list){new MutationObserver(()=>setTimeout(syncDownloads,0)).observe(list,{childList:true,subtree:true})}window.addEventListener('gyx:orders-changed',()=>setTimeout(syncDownloads,20));window.addEventListener('gyx:languagechange',()=>setTimeout(syncDownloads,20));document.addEventListener('member:opened',e=>{if(e.target?.id==='downloads')setTimeout(syncDownloads,0)})}function init(){fixReturnOrders();watch();setTimeout(syncDownloads,250);setTimeout(syncDownloads,1000)}document.readyState==='loading'?document.addEventListener('DOMContentLoaded',init,{once:true}):init()})();