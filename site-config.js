(()=>{'use strict';async function apply(){const c=window.gyxSupabase;if(!c)return;try{const {data,error}=await c.from('site_settings').select('key,value').eq('is_public',true);if(error||!Array.isArray(data))return;const s=Object.fromEntries(data.map(x=>[x.key,x.value]));if(s.site_name){document.querySelectorAll('.brand span:last-child').forEach(el=>el.textContent=s.site_name);document.title=document.title.replace('全球优选',s.site_name)}if(s.support_telegram){document.querySelectorAll('a[href*="t.me/"]').forEach(a=>a.href=s.support_telegram);const u=s.support_telegram.split('/').filter(Boolean).pop();document.querySelectorAll('.support-link.telegram small').forEach(el=>el.textContent=u?'@'+u.replace(/^@/,''):'Telegram')}if(s.support_email){document.querySelectorAll('a[href^="mailto:"]').forEach(a=>a.href='mailto:'+s.support_email);document.querySelectorAll('.support-link.email small').forEach(el=>el.textContent=s.support_email)}if(s.background_music_url){const audio=document.getElementById('backgroundMusic');if(audio&&audio.getAttribute('src')!==s.background_music_url){audio.setAttribute('src',s.background_music_url);audio.load()}}window.GYX_PUBLIC_SETTINGS=Object.freeze({...s})}catch(e){console.warn('site settings unavailable',e)}}if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',apply,{once:true});else apply()})();